---
name: feynman-exam
description: 用费曼法和小白追问做学习验收：要求无资料讲解，根据用户原话追问，识别漏洞并写出 exam.md。在用户说验收、考我、讲给你听、费曼、小白模式时使用。
---

# Feynman Exam

验收 skill。先读当前会话的 `plan.md` 和 `AGENTS.md` 中的验收规则。

## 步骤

1. 提醒用户不看原资料。
2. 用 Examiner 口吻，让用户按学习目标连续讲解 Core 项。先听完，再追问。
3. 切到 Novice：完全不懂该主题、但有正常逻辑。只根据用户刚说的内容问下一问。
4. 优先追模糊词、假因果、只复述定义、以及「自动 / 保证 / 一定」这类断言。
5. 连续若干轮没有新的 Core 漏洞后结束。不要为了提问而提问。
6. 用 `templates/exam.md` 写 `sessions/<date>-<topic>/exam.md`。
7. 回写 `_index.md`：达标则等级按本报告、巩固态 `fragile`；未达标则巩固态保持 `none`，状态 `remedial`。
8. 达标并沉淀时，为 Core 项补卡片（若还没有），`consecutive_success: 0`，`due` 从次日或按新卡规则起算。
9. **不要**改 `profile.md`。验收过关不是画像升级。

## 禁止

- 「你明白了吗？」
- 用户说懂了就判掌握
- 用选择题代替讲解验收
- 把 Optional 细节当成不过关理由
- 讲完标准答案后立刻宣布 L3/L4
- 因为「学完了 / 复了几次」改画像

掌握等级只看当场表现。只会背定义，最高 L1。
