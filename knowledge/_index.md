# Knowledge Index

巩固态：`none` 未验收通过 / `fragile` 刚学会或最近复习吃力 / `holding` 开始记住 / `stable` 过得了当前策略的时间考验。

策略：`fast` 需连续 2 次 `good`/`easy` 且间隔 ≥ 7 天；`strict` 同上但间隔 ≥ 21 天。补基础默认 `fast`。

到期：该主题卡片里最早的 `due`。逾期：`due < 今天` 的卡片张数。没有卡片则到期为 `—`，逾期为 `0`。

| 主题 | 目录 | 维度 | 目标 | 等级 | 巩固 | 策略 | 到期 | 逾期 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| （尚无） | | | | | none | fast | — | 0 | |

维度用：`计算机基础` / `后端工程` / `AI`。  
状态用：`planning` / `learning` / `examining` / `remedial` / `retained`。
