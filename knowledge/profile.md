# Profile

- 更新于：2026-09-10
- 职业背景：后端
- 当前目标：补计算机基础，为学 AI 做准备
- 一句话：基础还不牢固的后端，正在补计算机基础
- 默认巩固策略：`fast`（连续 2 次 `good`/`easy`，且 `interval_days ≥ 7`）
- 严格策略（更难主题再用）：`strict`（连续 2 次 `good`/`easy`，且 `interval_days ≥ 21`）

当前补基础主题默认用 `fast`。以后深入更难的知识时，把该主题在 `_index.md` 的「策略」改成 `strict`。

## 维度

| 维度 | 等级 | 来源 | 依据 |
| --- | --- | --- | --- |
| 计算机基础 | L1 | 陈述 | 尚无 `stable` 主题 |
| 后端工程 | L3 | 陈述 | 工作实践，本仓库尚未验收 |
| AI | L0 | 陈述 | 尚未开始 |

## 相对上次的变化

- 2026-09-10 建档。画像尚未被验收驱动改写。
